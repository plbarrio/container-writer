---
container-strip:
  - marginnoteopen
  - marginnoteclosed
---

# Strip — plain class names only

Blacklist entries are plain class names. Both classes below are listed
explicitly and should be stripped:

A paragraph with [open note]{.marginnoteopen} stripped inline.

A paragraph with [closed note]{.marginnoteclosed} stripped inline.

This span is not blacklisted and should remain: [keep me]{.sidebar}
